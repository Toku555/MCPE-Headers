#pragma once
class BlockBlobFeature{
	public:
		BlockBlobFeature(BlockID,int);
		BlockBlobFeature(BlockID,int);
		void place(BlockSource &,BlockPos const&,Random &);
		void place(BlockSource &,BlockPos const&,Random &);
		void ~BlockBlobFeature();
		void ~BlockBlobFeature();
};
