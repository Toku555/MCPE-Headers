#pragma once
class LightTexture{
	public:
		LightTexture(TexturePair &);
		LightTexture(TexturePair &);
		void getColorForUV(BrightnessPair);
		void getColorForUV(BrightnessPair);
		void refresh(LightTextureData const&,float,float);
		void refresh(LightTextureData const&,float,float);
};
